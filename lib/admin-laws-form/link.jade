- link = link || {}

.law-link(data-link=link.id)
  .link-row
    .link-group.link-id
      label=t('admin-laws-form.link.label.text')
      input.form-control(type='text', name='links[#{link.id}][text]', value=link.text)
    .link-group.link-url
      label=t('admin-laws-form.link.label.url')
      input.form-control(type='text', name='links[#{link.id}][url]', validate='url', value=link.url)
  a.remove-link.btn.btn-danger.btn-xs(href='#')= t('admin-laws-form.link.button.delete')
