#forums-wrapper.hide
  .forums-settings
    .heading
      - if(user.privileges.canCreate)
        .actions
          a.btn.btn-default(href='/forums/new')
            = t('newsfeed.call-to-action.start-a-forum')
      h1= t('settings.sidebar.forums')
    .forums
