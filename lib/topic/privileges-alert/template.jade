.privileges-alert
  .inner-container
    - if (visibility === 'closed' && !privileges.canVoteAndComment)
      .alert.alert-warning.with-icon
        i.alert-icon.glyphicon.glyphicon-lock
        = t('privileges-alert.not-allowed')
