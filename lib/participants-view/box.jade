- var plural = 1 === participants.length ? '' : 's'
- var more = participants.length > 5;
.participants-box
  span #{participants.length} #{t('Participant')}#{plural}
  a.view-more(href="#", style="padding-left: 6px;", class= more ? '' : 'hide')= t('view more')
